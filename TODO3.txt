TODO
----
SArrAss id e1 e2            
SNewArrAss id t exp         
SForEach t id exp stm1
IArrInit id t2 exp          X
EArrLen id                  X
EArrInd id e                X


Om vi får något index-out-of-bounds-liknande error när vi har stöd för for-each
kan det bero på att size inte funkar.

Prova att kommentera bort anropet till calloc för att se om listan vi sedan
allocerar verkligen använder minnet vi skapar i calloc.

Innehåller Makefile allt som behövs för labb C? Är det något nytt sedan labb B?


Gör internal stm för free(a)?

Skapa funktion som anropar newLocVar och en som anropar extendVar, båda med
inparameter "t".

Tips
----
Arrayerna vi använder i LLVM kallas "Pascal style"

How to use calloc:
  ¤ http://www.wilfred.me.uk/blog/2015/02/21/my-first-llvm-compiler/
  ¤ http://www.cplusplus.com/reference/cstdlib/calloc/
    "void* calloc (size_t num, size_t size);
    Allocate and zero-initialize array.
    Allocates a block of memory for an array of num elements, each of them size 
    bytes long, and initializes all its bits to zero."
